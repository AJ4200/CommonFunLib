import EndpointList from "@/components/EndpointList";
import endpoints from "@/lib/endpoints";

const CommonEndpointList = () => <EndpointList endpoints={endpoints} />;

export default CommonEndpointList;
