# Deprecated Java Source

The Java implementation in this folder is deprecated and kept only for historical reference.

Current CommonFunLib development happens in:

- `cfl-api` for the Express API and npm package.
- `cfl-web` for the playground and documentation app.

Use the npm package or API server for new projects.
