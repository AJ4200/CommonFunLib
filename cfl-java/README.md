# Deprecated Java Library

This Java implementation is deprecated and frozen for historical reference. It
does not receive new features, including QR code generation and link
slugification.

Use `cfl-api` for the Express API and npm package, or `cfl-web` for the
playground and documentation. The Java public classes are annotated with
`@Deprecated` so Java compilers and IDEs show the migration warning.